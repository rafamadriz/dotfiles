theme = vesper
mouse-hide-while-typing = true

# don't confirm when closing window/terminal
confirm-close-surface = false

# disable all ligatures
font-feature = -liga
font-feature = -calt
font-feature = -dlig

# only works on macos
# keybind = global:ctrl+shift+]=toggle_quick_terminal
